# 版本记录

## 2026-07-02

- 新增 PiPerm 权限控制实现文档，说明 Pi extension 工作原理、当前项目生效流程、配置链路、工具拦截流程和 Mermaid 流程图。
- 将 `core/` 与 `test/` 从 JavaScript 迁移为 TypeScript，测试脚本改为通过 `tsx` 运行。
- 将操作权限配置调整为原命令模式优先，例如 `git push`、`rm -r`、`curl | sh`。
